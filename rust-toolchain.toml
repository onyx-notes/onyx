[toolchain]
channel = "1.96.1"
components = ["rustfmt", "clippy"]
# Mobile targets (Android device + emulator, iOS device + simulator).
targets = [
    "aarch64-linux-android",
    "x86_64-linux-android",
    "aarch64-apple-ios",
    "aarch64-apple-ios-sim",
]
